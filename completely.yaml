sshm0:
- list
- add
- connect
- cp
- edit
- plugin
- remove
- rename
- show
- export
- ping
- doctor
- --config-dir
sshm0 list:
- --tag
- -t
- --filter
- -f
- --long
- -l
- --recent
- -r
sshm0 add:
- --password
- --pass
- --pwd
- -p
- --key
- -i
- --auth
- -a
- --port
- -P
- --force
- --no-force
- -f
- --connect
- --no-connect
- -c
- --tag
- -t

sshm0 add*--key:
- $(dir="$HOME/.ssh"; [ -d "$dir" ] && /usr/bin/ls "$dir" || true)
sshm0 connect:
- $(dir="${SSHM0_CONFIG_DIR:-$HOME/.config/sshm0}/servers"; [ -d "$dir" ] && /usr/bin/ls "$dir" || true)
- --cd
- --exec-before
sshm0 cp:
- <file>
- <file>
sshm0 edit:
- $(dir="${SSHM0_CONFIG_DIR:-$HOME/.config/sshm0}/servers"; [ -d "$dir" ] && /usr/bin/ls "$dir" || true)
- --ip
- --user
- --password
- -p
- --key
- -i
- --auth
- --port
- --connect
- --no-connect
- -c
- --tag
- -t

sshm0 edit*--key:
- $(dir="$HOME/.ssh"; [ -d "$dir" ] && /usr/bin/ls "$dir" || true)
sshm0 export:
- --output
- -o
- --merge
sshm0 plugin:
sshm0 remove:
- $(dir="${SSHM0_CONFIG_DIR:-$HOME/.config/sshm0}/servers"; [ -d "$dir" ] && /usr/bin/ls "$dir" || true)
sshm0 rename:
- $(dir="${SSHM0_CONFIG_DIR:-$HOME/.config/sshm0}/servers"; [ -d "$dir" ] && /usr/bin/ls "$dir" || true)
sshm0 show:
- $(dir="${SSHM0_CONFIG_DIR:-$HOME/.config/sshm0}/servers"; [ -d "$dir" ] && /usr/bin/ls "$dir" || true)
sshm0 ping:
- $(dir="${SSHM0_CONFIG_DIR:-$HOME/.config/sshm0}/servers"; [ -d "$dir" ] && /usr/bin/ls "$dir" || true)
sshm0 doctor:



